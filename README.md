# Predict-the-Optimized-Value
Design an Artificial Neural Network (ANN) and a Convolutional Neural Network (CNN) structure to predict the optimized value. Train the models using a cross-validation technique.
